[server]
headless = true
address = "0.0.0.0"
port = 5000
enableXsrfProtection = false  # 👈 Add this line
enableCORS = false            # 👈 Only works if XSRF protection is off

[theme]
primaryColor = "#4CAF50"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F0F8FF"
textColor = "#262730"

